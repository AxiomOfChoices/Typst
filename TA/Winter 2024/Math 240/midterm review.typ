#import "/Templates/generic.typ": latex,header
#import "/Templates/math.typ": *
#show: latex
#show: doc => header(doc, title: "Midterm Review")
#set page(margin: (top: 4cm))
= Section 1 - Sets
== Question 1
For each of the following sets denoted in set builder notation, describe this set and write 5 of its elements.

#box(height: 38pt)[
  #columns(3, gutter: 11pt)[
    #set par(justify: true)
    + ${ 4 y + 2 : ZZ }$
    + ${ x in ZZ : -5 <= x <= -2 }$
    + ${ x in RR : x^2 = 3 }$
    + ${ $
    +
    +
    +
    +
    +
  ]
]
